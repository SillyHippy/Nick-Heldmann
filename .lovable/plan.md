

# Update Email and Favicon

## 1. Update Email Address
Change all instances of `Njheldmann@gmail.com` to `Nickheldmannbridges@gmail.com` in:
- `src/components/HeroSection.tsx` (hero contact link)
- `src/components/FooterSection.tsx` (footer contact link)

## 2. Custom Favicon
Since no image was provided, we'll create a unique SVG favicon featuring Nick's initials "NH" styled with the site's warm color palette. This will be an inline SVG favicon in `index.html` — no external file needed, and it'll look crisp at any size.

The favicon will use the site's primary navy/warm tone with clean typography for "NH", giving it a personal brand feel that matches the portfolio.

### Files to modify
- `index.html` — replace the generic favicon with an inline SVG favicon
- `src/components/HeroSection.tsx` — update email address
- `src/components/FooterSection.tsx` — update email address

